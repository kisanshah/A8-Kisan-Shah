<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.resumebuilder">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ResumeBuilder">
        <activity android:name=".views.EditActivity">

        </activity>
        <activity android:name=".views.ExportActivity" >

        </activity>
        <activity android:name=".views.ResetActivity" />
        <activity android:name=".views.SignInActivity">

        </activity>
        <activity android:name=".views.SignUpActivity" />
        <activity android:name=".views.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>